# OLX

## Description
This project is designed for collecting data from olx.ua. The program runs every minute. Database dump is created once a day to folder /backups/*.sql

---

## How to Run the Service
To start the service, use the following commands:

> docker-compose build

> docker-compose up -d